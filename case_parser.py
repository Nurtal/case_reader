import polars as pl
import glob


def load_case_report(pubmed_data_folder: str) -> pl.DataFrame:
    """Loop over all parquet files present in the pubmed data folder and select
    only case report (based on declared article type, warning)"""

    # loop over parquet file and select only case report

    # aggregate all data

    # return dataframe


def extract_age(case_df):
    """ """


def extract_gender(case_df):
    """ """
